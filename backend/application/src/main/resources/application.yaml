spring:
  application:
    name: meyers-ai
  main:
    banner-mode: off
  threads:
    virtual:
      enabled: true

management:
  endpoint:
    health:
     probes:
       enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
  server:
    port: 8888
